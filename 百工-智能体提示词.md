# 百工智能体提示词 —— RPG 文字冒险（单工具 · 引擎驱动版）

**给「骰子之神」这类智能体贴的提示词只有 306 字**（早先的 1183 字版已废弃，因为那是重复劳动）。

## 为什么能这么短

三层分工，各管一段：

| 层 | 负责 | 在哪 |
|---|---|---|
| **工具描述** | 一轮的流程（createGame → progressStory → promptUserActions → selectAction → updateGame） | MCP `tools/list` 的 `description`，模型每次都看得到 |
| **工具返回值** | 这一步之后具体该调什么、带什么参数 | 网关把上游的 `🎯 Next Step` 翻成中文并改写成 `调用 rpg(action="progressStory")，参数：…` |
| **system prompt** | 只放不会变、错了就致命的规矩 | 下面这 306 字 |

**关键点**：上游原生返回的指引写的是 `Call the 'updateGame' tool ...`，合并成单工具后**工具名已经不存在**，模型照做必失败。所以网关必须重写这段指引——这也是"提示词能瘦下来"的前提。

---

## 直接贴进智能体的提示词（306 字）

```
你是跑团主持人，带用户玩中文文字跑团。规则判定全在服务端的 rpg 工具里，你只负责叙事与拟选项。

只有 1 个工具 rpg，用 action 选操作。每一步该做什么，工具返回的「🎯 下一步」已经写明，照着做即可，不用背流程。

三条铁律：
1. 每一步都把上一步返回的 gameId 原样回传，写错一个字这局就丢了。
2. 摆给玩家的选项用你传给 promptUserActions 的那几条原文；玩家回「2」就把第 2 条原文放进 selectedOption。
3. 工具报错时不要假装成功，用大白话告诉玩家，再按提示重试或重开。

像个有经验的主持人：一轮几句话，选项一律按 1./2./3. 给玩家。
```

---

## 工具侧（网关）承担的事，供排查时对照

| action | 必填参数 | 网关额外做的 |
|---|---|---|
| `createGame` | `initialStateInJson` | 返回里给 `局号:`，提示下一步 progressStory |
| `progressStory` | `gameId`, `progress` | 中文处境、中文下一步 |
| `promptUserActions` | `gameId`, `options`（你拟 2~4 条） | 剥掉 `ui://` HTML，转成 `1) 2) 3)` 可读选项并记住原文 |
| `selectAction` | `gameId`, `selectedOption` | **接受「第2个」「2」这类写法**，自动换算 `selectedIndex` |
| `updateGame` | `gameId`, `fieldSelector`, `value` | 变更叙述中文化 |
| `getGame` / `selectRestart` | `gameId` | 只读 / 重开说明中文化 |

报错一律是中文一句话（如「❌ 调用失败（action=getGame）：局面 xxx 不存在（服务重启过…）—— 请用 createGame 重开一局」），模型不需要翻译英文报错。

---

## 运维注意

- 免费实例原本闲置 15 分钟会休眠、冷启动 20~60 秒（实测界面显示「插件 25.818s」就是这个）。网关已加**自唤醒心跳**（每 10 分钟自请求 `/healthz`）。
- 局面存在网关常驻子进程的内存里：**实例重启 = 进行中的局作废**，模型会收到中文提示并引导重开。
- 平台侧只暴露 1 个工具；旧的 7 个工具名在网关里仍兼容（向后兼容），但平台上已不可见，模型不该再直接用它们。
